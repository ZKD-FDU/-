# 数据卡

## 数据来源

当前 MVP 使用合成县域数据，由 `generate_qingyuan` 按随机种子生成。系统不包含真实个人身份信息。

## 标签

- `SYNTHETIC`：合成人口、机构、基础设施、网络。
- `SIMULATED`：运行指标、个体状态、事件、决策轨迹、政策比较。

## 适用范围

适用于演示多智能体政策推演流程、比较不同转移组织方案、暴露资源与路线断点。

## 不适用范围

不应直接作为真实县域调度依据；真实部署前需要接入水文、地理、人口台账、机构名册、避难点容量、车辆与照护资源等事实数据，并完成校准。
